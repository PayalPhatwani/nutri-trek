#If a folder contains Python files and you import from it → add __init__.py. that's why all folder contains __init__.py file.
#An __init__.py file can be empty, but it indicates that the directory it is present in is a Python package,
# so Python treats the directory as a package and allows you to import modules from it.